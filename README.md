# review-router

A GitHub App that assigns PR reviewers fairly by load. Webhook in, fast ack, real work in a queued job, correct under duplicates and concurrent opens.

This is a learning project. The point is event driven backend skill (webhooks, queues, concurrency control) that my other projects do not cover.
